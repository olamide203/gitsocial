{
  "name": "gitsocial",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm dlx lerna run dev --scope=backend",
    "dev:frontend": "pnpm dlx lerna run dev --scope=frontend",
    "dev": "pnpm dlx lerna run dev",
    "build:frontend": "pnpm dlx lerna run build --scope=frontend"
  },
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
